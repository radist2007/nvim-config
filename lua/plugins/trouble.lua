-- lua/plugins/trouble.lua
-- TODO: розглянути більш детально
-- Призначення: Цей файл налаштовує плагін trouble.nvim. Цей плагін надає дуже зручний інтерфейс (зазвичай у вигляді списку в окремому вікні або внизу) для перегляду та навігації по:
-- Діагностиці LSP (помилки, попередження, інформація, підказки).
-- Результатах пошуку Telescope.
-- Списках Quickfix та Location list.
-- LSP Посиланнях, Визначеннях, Реалізаціях. LazyVim включає та налаштовує його за замовчуванням.
-- Поточна Конфігурація:
-- Ти додав enable = true. Це не обов'язково, оскільки LazyVim і так вмикає Trouble, але це явно показує твій намір його використовувати. ✅
-- opts = { use_diagnostic_signs = true }: Ця опція гарантує, що поруч з кожним записом у списку Trouble будуть відображатися іконки, що позначають рівень діагностики (помилка, попередження тощо). Це налаштування за замовчуванням у LazyVim теж зазвичай true, але явне встановлення нічому не шкодить. ✅
-- Як Користуватися (у LazyVim): Зазвичай Trouble відкривається за допомогою комбінацій клавіш, що починаються з <leader>x. Натисни <leader>x і подивися у підказку which-key:
-- <leader>xx: Показати діагностику поточного документа.
-- <leader>xX: Показати діагностику всього робочого простору (проекту).
-- <leader>xL: Показати Location List.
-- <leader>xQ: Показати Quickfix List.

return {
  -- Налаштування Trouble
  {
    "folke/trouble.nvim",
    -- opts об'єднаються з налаштуваннями LazyVim за замовчуванням
    opts = {
      use_diagnostic_signs = true, -- Показувати іконки діагностики (помилка/попередження) всередині списку Trouble
    },
  },
}
